# Another Chat TG Bot (Rust)

Универсальный Telegram-бот с ИИ-ассистентом, поддерживающий множество моделей, мультимодальность и расширенный **Режим Агента** с выполнением кода. **Написан на Rust.**

## Описание

Этот проект представляет собой Telegram-бота, который интегрируется с различными API больших языковых моделей (LLM) для предоставления пользователям многофункционального ИИ-ассистента. Бот может обрабатывать текстовые, голосовые, видео сообщения и изображения, работать с документами, управлять историей диалога и выполнять сложные задачи в изолированной песочнице.

Бот разработан с использованием **Rust**, библиотеки `teloxide`, `rusoto_s3` для взаимодействия с Cloudflare R2, и нативной интеграции с провайдерами ИИ (Groq, Mistral AI, Google Gemini, OpenRouter, Zai).

## Возможности

*   **🤖 Режим Агента (Agent Mode):**
    *   **Интегрированная песочница:** Безопасное выполнение Python-кода и Bash-команд в изолированных Docker-контейнерах.
    *   **Инструменты (Tools):** Чтение/запись файлов, выполнение команд, сложные вычисления.
    *   **Автономность:** Агент сам решает, какие инструменты использовать для решения задачи.
    *   **Отдельная авторизация:** Возможность ограничить доступ к агенту (через `AGENT_ACCESS_IDS`) отдельно от основного списка пользователей бота.
    *   **Долгая память и контекст:** Поддержка длинных цепочек рассуждений с автоматическим сжатием контекста.
    *   **Прогресс выполнения:** Интерактивное отображение текущего шага работы агента в Telegram.
*   **Поддержка множества LLM:** Интеграция с Groq, Mistral AI, Google Gemini, OpenRouter и Zai.
*   **Нативный Tool Calling:** Эффективное использование инструментов в моделях (например, Mistral Large, Devstral).
*   **Обработка различных типов ввода:**
    *   Текстовые сообщения.
    *   Голосовые сообщения (распознавание речи через Gemini).
    *   Видео сообщения (извлечение аудио и распознавание).
    *   Изображения (анализ и описание через мультимодальные модели).
*   **Управление контекстом:**
    *   Сохранение истории диалога в Cloudflare R2 (S3-совместимое хранилище).
    *   Возможность очистки контекста или изменения системного промпта.
*   **Развертывание с Docker:** Полная изоляция приложения и среды выполнения агента.
*   **Высокая производительность:** Rust обеспечивает минимальный оверхед и надежную работу под нагрузкой.

## Необходимые условия

*   **Docker:** Обязателен для работы приложения и песочницы агента.
*   **Cloudflare R2:** Бакет для хранения данных пользователей и истории.
*   **API Ключи:** Как минимум один из поддерживаемых LLM провайдеров (рекомендуется OpenRouter или Gemini).

## Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/0FL01/Another-Chat-with-LLM.git
    cd Another-Chat-with-LLM
    ```

2.  **Настройте переменные окружения:**
    Создайте `.env` на основе `.env.example`.

3.  **Соберите образ песочницы:**
    ```bash
    cd rust-src/sandbox
    docker build -t agent-sandbox:latest -f Dockerfile.sandbox .
    cd ../..
    ```

4.  **Соберите и запустите бота:**
    ```bash
    docker-compose up --build -d
    ```

## Конфигурация (.env)

```dotenv
# Telegram
TELEGRAM_TOKEN=ВАШ_ТОКЕН
ALLOWED_USERS=ID1,ID2 # Список разрешенных Telegram ID (базовый доступ)
AGENT_ACCESS_IDS=ID1 # Доступ к Режиму Агента (тратит много токенов)

# Cloudflare R2 (S3)
R2_ACCESS_KEY_ID=...
R2_SECRET_ACCESS_KEY=...
R2_ENDPOINT_URL=...
R2_BUCKET_NAME=...

# API Keys
GROQ_API_KEY=...
MISTRAL_API_KEY=...
GEMINI_API_KEY=...
OPENROUTER_API_KEY=...
ZAI_API_KEY=... # Новый провайдер Zai
```

## Использование

1.  Отправьте `/start` боту.
2.  **Обычный режим:** Просто пишите сообщения или отправляйте файлы/голосовые.
3.  **🤖 Режим Агента:** Нажмите кнопку "🤖 Режим Агента". Теперь бот сможет выполнять код. Например: *"Напиши скрипт на python, который скачивает главную страницу google и найди там слово 'Search'"*.
4.  **Управление:** Используйте кнопки "Очистить контекст", "Сменить модель" или "Доп функции".

## Структура проекта

```text
.
├── rust-src/
│   ├── src/
│   │   ├── agent/    # Логика Агента (сессии, инструменты, память)
│   │   ├── bot/      # Обработчики Telegram (handlers, state)
│   │   ├── llm/      # Провайдеры ИИ (Mistral, Gemini, Zai и др.)
│   │   ├── sandbox/  # Управление Docker-песочницей
│   │   ├── storage.rs # Работа с S3/R2
│   │   ├── config.rs # Настройки и список моделей
│   │   └── main.rs   # Точка входа
│   └── sandbox/      # Ресурсы для образа песочницы
├── Dockerfile        # Dockerfile основного приложения
└── docker-compose.yml
```

## Доступные модели

| Название | Провайдер | Особенности |
| :--- | :--- | :--- |
| **OR Gemini 3 Flash** | OpenRouter | Мультимодальность, модель по умолчанию |
| **ZAI GLM-4.7** | Zai | Высокая точность, большой контекст |
| **Mistral Large** | Mistral | Нативный вызов инструментов |
| **Devstral 2512** | Mistral | Оптимизирована для кодинга и работы Агента |
| **GPT-OSS-120b** | Groq | Очень высокая скорость |

## Разработка

Для локальной разработки (требуется установленный Rust):
```bash
cd rust-src
cargo run
```

## Лицензия

MIT License
