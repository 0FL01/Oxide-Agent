use teloxide::prelude::*;

fn main() {
    // Test if forum topic methods are available
    // This should fail to compile if they don't exist
    // create_forum_topic(chat_id, name)
    // edit_forum_topic(chat_id, thread_id, ...)
    // close_forum_topic(chat_id, thread_id)
    // reopen_forum_topic(chat_id, thread_id)
    // delete_forum_topic(chat_id, thread_id)
    // get_forum_topic(chat_id, thread_id)
    let _ = "compile_check";
}
