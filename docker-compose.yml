services:
  another_chat_tg:
    build: .
    image: another_chat_tg:latest
    container_name: another_chat_tg
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./allowed_users.txt:/app/allowed_users.txt
