services:
  another_chat_tg:
    #build: .
    image: 0fl01/another-chat-tg:1.0.1
    container_name: another_chat_tg
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./allowed_users.txt:/app/allowed_users.txt
      - ./logs:/app/logs
