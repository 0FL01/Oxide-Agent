# Goal: Modular Architecture Refactor

Date started: 2026-05-23
Status: active
Codex goal: Implement the modular architecture refactor described in prd/PRD.md on branch modular-arch. Track progress in this repo-local goal document, proceed milestone by milestone, and commit after each completed major checkpoint.

## Objective

Refactor Oxide Agent into the capability-oriented modular architecture specified by `prd/PRD.md`.

Done when build profiles deterministically select compiled modules, heavy dependencies are eliminated from profiles that do not select them, runtime config can only enable compiled modules, tool/provider/storage/sandbox/transport registration flows through a single capability registry path, Docker/Compose assets match the selected profile, and the PRD acceptance criteria are validated or explicitly documented as remaining work.

## Scope

In scope:
- Add capability/module foundations, manifests, deterministic compiled module lists, and runtime validation.
- Replace broad/global provider and tool registration with feature-gated modules.
- Move LLM providers, tools, storage, sandbox, transports, MCP/media/search/browser integrations, reminders, file delivery, and manager groups toward explicit capability modules.
- Make Cargo features atomic and compose profile features from them.
- Move heavyweight dependencies behind optional features and add leakage checks.
- Make Docker, Compose, sandbox images, and optional binaries profile-aware.
- Remove legacy compatibility paths, deprecated aliases, embedding/skill runtime, duplicate registries, and old migration-only code as replacement modules land.

Out of scope:
- Preserving old config compatibility or old environment variable aliases.
- Deployment migrations for existing state.
- Enterprise-scale orchestration, sharding, HA, queueing, or extra observability.
- Adding new product features unrelated to modular architecture.
- Keeping local filesystem or SQLite as durable storage.

## Repository Context

- PRD: `prd/PRD.md`.
- Branch for this work: `modular-arch`.
- Existing workspace crates: `oxide-agent-core`, `oxide-agent-runtime`, `oxide-agent-transport-telegram`, `oxide-agent-transport-web`, `oxide-agent-telegram-bot`, `oxide-agent-sandboxd`.
- Initial feature baseline: `oxide-agent-core` had coarse features (`tavily`, `searxng`, `browser_use`, `jira`, `mattermost`) and default features enabled.
- Initial heavy dependency leakage examples: AWS SDK, RMCP, Bollard, async-openai, the former direct Google Gemini SDK, ZAI, reqwest, tar/bincode/sandbox dependencies were core-level dependencies.
- Existing goal convention: repo-local files in `docs/goals/`.

## Implementation Plan

1. Phase 0: Create this goal document from `prd/PRD.md` and commit it.
2. Phase 1: Add Milestone 1 dependency/feature audit artifacts: dependency classification, atomic feature naming map, profile map, and first leakage-check script/CI-friendly commands.
3. Phase 2: Convert Cargo feature defaults toward `default = []`, introduce initial atomic/profile features, and move obvious heavy dependencies to optional features where code already has or can cheaply get cfg boundaries.
4. Phase 3: Add capability foundation types: module IDs, capability IDs, manifest structs, deterministic compiled module list scaffolding, duplicate detection, and tests.
5. Phase 4: Add config validation against compiled module manifests and CLI/test hooks to print compiled/enabled capabilities.
6. Phase 5: Start provider modularization with one narrow provider slice, then repeat provider-by-provider.
7. Phase 6: Start tool modularization with low-risk atomic tools, then split sandbox/search/MCP/media/manager tools by capability.
8. Phase 7: Modularize storage around S3/R2 durable storage and remove concrete storage construction from transport startup.
9. Phase 8: Modularize sandbox backend/tool split and gate sandbox daemon/broker/Docker dependencies.
10. Phase 9: Decouple transport startup from app composition and introduce profile-aware bootstrap.
11. Phase 10: Make Docker/Compose/sandbox images profile-aware and remove unconditional sidecars/binaries/assets.
12. Phase 11: Add final profile matrix, dependency leakage, manifest snapshot, config validation, Docker/Compose, and size-budget checks.

## Validation Contract

- Formatting: `cargo fmt --all --check`.
- Baseline workspace check during early phases: `cargo check --workspace`.
- Lint before finishing a code checkpoint: `cargo clippy --workspace --all-targets --all-features`.
- Profile checks as they become available:
  - `cargo check --workspace --no-default-features --features profile-embedded-opencode-local`
  - `cargo check --workspace --no-default-features --features profile-no-sandbox`
  - `cargo check --workspace --no-default-features --features profile-search-only`
  - `cargo check --workspace --no-default-features --features profile-lite`
  - `cargo check --workspace --no-default-features --features profile-media-enabled`
  - `cargo check --workspace --no-default-features --features profile-full`
- Dependency leakage checks as they become available:
  - embedded/profile-specific builds must not include unselected AWS SDK, RMCP, Bollard, or unrelated provider SDKs;
  - no-sandbox builds must not include Bollard or sandbox broker protocol;
  - no-MCP/search-only builds must not include RMCP.

Done when:
- PRD section 20 acceptance criteria are satisfied.
- Required profile builds are reproducible from Cargo features.
- Compiled and enabled capabilities are emitted as deterministic JSON.
- Config schema/example generation exists for compiled modules.
- Docker/Compose assets correspond to selected modules.
- CI or CI-ready scripts prove dependency absence and profile topology.
- Legacy registry/compatibility paths are deleted rather than wrapped.

## Decisions

- 2026-05-23: Treat the PRD as a multi-milestone architecture migration, not a single patch.
- 2026-05-23: Commit after every completed phase or major checkpoint.
- 2026-05-23: Use branch `modular-arch` for all work in this goal.
- 2026-05-23: Treat PRD section 22.7 as authoritative over earlier local-storage examples: S3/R2-compatible object storage is the only durable storage target; local filesystem is transient only.
- 2026-05-23: Keep implementation pragmatic for personal-scale usage; avoid extra orchestration/HA layers unless the PRD explicitly requires a modular boundary.
- 2026-05-23: Remove the direct Google Gemini provider and SDK from the project. Gemini-family model IDs remain supported only through OpenRouter routes.

## Progress Log

- 2026-05-23 13:38 +03: Read `prd/PRD.md`, confirmed branch `modular-arch` with clean working tree, created active Codex goal, and started this repo-local goal document. Next: commit Phase 0, then implement Phase 1 dependency/feature audit artifacts.
- 2026-05-23 13:49 +03: Phase 1 added PRD-style atomic/profile feature names, set `oxide-agent-core` default features to empty, renamed existing feature gates from `tavily`/`searxng`/`browser_use`/`jira`/`mattermost` to canonical `tool-*` and `integration-*` names, added profile TOML stubs, dependency audit docs, a CI workflow for profile checks, and `scripts/check-cargo-tree-deny.sh`. Validation passed: `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Expected leakage evidence captured: `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `profile-search-only`, and `llm-opencode-go` fail because AWS SDK, Bollard, RMCP, provider SDKs, and broker deps are still unconditional. Next: Phase 2 optional dependency gates and cfg boundaries for the first heavy module slice.
- 2026-05-23 14:00 +03: Phase 2a removed the inactive legacy skills/embeddings subsystem per PRD section 22.11. Deleted `agent/skills/*`, `llm/embeddings.rs`, LLM embedding fields/methods, embedding config/env helpers, root markdown `skills/`, Docker `COPY skills/`, and stale README/env references. Prompt assembly now clears legacy loaded-skill accounting without accepting a `SkillRegistry`. Static check finds no production references to `EmbeddingProvider`, `EmbeddingTaskType`, `generate_embedding`, `probe_embedding_dimension`, `SkillRegistry`, `SkillMatcher`, `EmbeddingService`, `EMBEDDING_*`, `SKILL_*`, `SKILLS_DIR`, or `COPY skills`. Validation passed: `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-no-sandbox`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Expected leakage remains: `scripts/check-cargo-tree-deny.sh llm-opencode-go` still fails because provider SDKs, AWS SDK, Bollard, and RMCP are not optional yet. Next: gate provider SDK modules behind `llm-*` features.
- 2026-05-23 14:10 +03: Phase 2b moved provider SDK crates behind provider features: `async-openai` behind `llm-groq`/`llm-mistral`, the then-present direct Google Gemini SDK behind its temporary feature, `zai-rs` behind `llm-zai`, and `claudius` behind `llm-minimax`. LLM provider modules and registration blocks are now cfg-gated by `llm-*`; `chatgpt-login` is behind `oxide-agent-telegram-bot/llm-chatgpt` and Docker full-profile build enables it explicitly. Validation passed: `cargo check --workspace`, `cargo check --workspace --no-default-features --features llm-opencode-go`, `profile-search-only`, and `profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Leakage improved: `scripts/check-cargo-tree-deny.sh llm-opencode-go` no longer reports the direct Gemini/ZAI/async-openai/claudius provider SDKs, and `profile-search-only` no longer reports provider SDK leaks. Remaining expected leaks: AWS SDK, Bollard/tar, RMCP, and sandbox broker deps. Next: split storage/sandbox/MCP dependency gates.
- 2026-05-23 14:39 +03: Phase 2c moved AWS/R2 SDK crates behind `storage-s3-r2` and gated the R2 storage implementation/export behind that feature. `StorageError` no longer exposes AWS SDK types, wiki-memory's direct `R2Storage` backend is cfg-gated, and the real Telegram runtime path now requires/forwards `storage-s3-r2` through transport and binary package features while the no-storage workspace build keeps only the runtime stub/config compiled. Docker full-profile builds use `oxide-agent-telegram-bot/profile-full`. Validation passed: `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `profile-search-only`, and `profile-no-sandbox`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check -p oxide-agent-telegram-bot --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Leakage improved: `scripts/check-cargo-tree-deny.sh llm-opencode-go` no longer reports AWS SDK crates; remaining expected leaks are `bollard`, `rmcp`, `bincode`, `serde_bytes`, and `tar` depending on profile. Next: split sandbox and RMCP dependencies.
- 2026-05-23 15:02 +03: Phase 2d moved sandbox Docker and broker dependencies behind sandbox feature gates: `bollard`, `tar`, `bytes`, `http-body-util`, `bincode`, and `serde_bytes` are no longer compiled for no-sandbox/search/opencode-only profiles. Added a no-backend `SandboxManager` stub to keep non-sandbox profiles buildable, gated `stack_logs` behind `tool-stack-logs`, and made `oxide-agent-sandboxd` require explicit `sandbox-daemon`/`profile-full` features. Docker full-profile builds now enable both `oxide-agent-telegram-bot/profile-full` and `oxide-agent-sandboxd/profile-full`. Validation passed: `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `profile-search-only`, and `profile-no-sandbox`, `cargo check -p oxide-agent-sandboxd --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Leakage improved: `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `profile-search-only`, and `llm-opencode-go` now fail only on `rmcp`. Next: split RMCP integrations.
- 2026-05-23 15:25 +03: Phase 2e moved `rmcp` behind `integration-mcp-jira`, `integration-mcp-mattermost`, and `integration-ssh-mcp`. Jira/Mattermost providers were already module-gated; SSH MCP now has a no-client stub for shared approval/preflight types so manager/topic code can compile without the upstream MCP SDK. Registry SSH tool registration is gated on `integration-ssh-mcp`, and the modular architecture CI leakage job is now enforcing rather than `continue-on-error`. Validation passed: `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Leakage checks now pass for `profile-no-sandbox`, `profile-search-only`, and `llm-opencode-go`. Next: web/search/browser ownership boundaries and capability registration.
- 2026-05-23 15:42 +03: Phase 2f moved `reqwest` and `htmd` behind explicit feature owners. `reqwest` is now optional and selected only by HTTP-using LLM/tool features; `htmd` is owned by `tool-webfetch-md`. `webfetch_md`, media-file, Kokoro TTS, and Silero TTS modules/exports/registrations are cfg-gated, manager tool catalog entries now respect those tool features, and sub-agent webfetch registration no longer leaks into `llm-opencode-go`. Validation passed: `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `profile-no-sandbox`, `profile-search-only`, `profile-lite`, `profile-embedded-opencode-local`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: commit this checkpoint, then start capability manifest scaffolding.
- 2026-05-23 16:10 +03: Phase 3a added the first capability manifest scaffold in `oxide-agent-core::capabilities`: stable module/capability IDs, `CapabilityKind`, `CapabilityModule`, static module descriptors, deterministic `compiled_modules()`, `CompiledCapabilityManifest`, `EnabledCapabilityManifest`, and a minimal `ModuleRegistry` shell. The compiled manifest is feature-gated across current atomic features and serializes to deterministic JSON; tests cover deterministic ordering plus duplicate module/capability ID failures. Validation passed: `cargo check --workspace`, all six profile checks, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, leakage checks for `llm-opencode-go`, `profile-no-sandbox`, and `profile-search-only`, `cargo test -p oxide-agent-core capabilities::manifest --all-features`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Note: a broad exploratory `cargo test -p oxide-agent-core capabilities --no-default-features --features llm-opencode-go` also matched old `llm::capabilities` tests and exposed an unrelated Nvidia capability assertion failure; the exact new manifest tests passed. Next: wire config validation/CLI output against the compiled manifest.
- 2026-05-23 16:25 +03: Phase 3b added the first CLI/debug output path for compiled capabilities: `oxide-agent-telegram-bot capabilities --compiled --json` prints the deterministic compiled manifest and exits before dotenv/logging/bot startup. Added parser tests for default bot startup, the compiled capabilities command, and partial command rejection. Validation passed: `cargo test -p oxide-agent-telegram-bot startup_command --no-default-features`, `cargo run -p oxide-agent-telegram-bot --bin oxide-agent-telegram-bot --no-default-features -- capabilities --compiled --json`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: add config validation against compiled module IDs.
- 2026-05-23 16:35 +03: Phase 3c added the reusable config validation primitive on `CompiledCapabilityManifest`: `contains_module_id` and `validate_configured_module_ids` reject runtime module config keys that are absent from the compiled manifest with `ManifestError::NonCompiledModuleConfig`. Added a focused test for compiled vs non-compiled module IDs. Validation passed: `cargo test -p oxide-agent-core capabilities::manifest --all-features`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: connect this primitive to parsed config and enabled-manifest selection.
- 2026-05-23 15:32 +03: Phase 3d connected module config parsing to enabled-manifest selection. `AgentSettings` now carries a `modules` map keyed by stable module ID and validates configured module IDs against the compiled manifest during startup. Added lightweight `ModuleRuntimeSettings` for CLI/config-only reads, `EnabledCapabilityManifest::from_compiled_module_ids`, and `CompiledCapabilityManifest::enabled_manifest_from_configured_modules`, using all compiled modules by default and subtracting modules with `enabled: false`. CLI now supports `oxide-agent-telegram-bot capabilities --enabled [--config PATH] --json` and rejects non-compiled module IDs. Validation passed: `cargo test -p oxide-agent-core capabilities::manifest --all-features`, `cargo test -p oxide-agent-core module_runtime_settings_deserialize_enabled_flags --all-features`, `cargo test -p oxide-agent-telegram-bot startup_command --no-default-features`, `cargo run -p oxide-agent-telegram-bot --bin oxide-agent-telegram-bot --no-default-features -- capabilities --enabled --json`, a negative temp-config CLI smoke with `tool/missing`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: begin routing one narrow provider/tool slice through the capability registry rather than only manifesting it.
- 2026-05-23 15:38 +03: Phase 4a started the first typed tool registration slice with `tool-todos`. Added `agent::tool_runtime::modules` with `ToolRuntimeModuleContext`, `ToolRuntimeModule`, and `TodosToolRuntimeModule`, then changed the typed runtime builder to register `write_todos` through that module instead of directly constructing `TodosProvider` in the global typed registry path. The legacy provider registry remains unchanged for now. Validation passed: `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: repeat this pattern for a second low-risk typed tool or wire module enablement into typed runtime registration.
- 2026-05-23 15:43 +03: Phase 4b wired runtime module enablement into the first typed tool module. Added `AgentSettings::is_module_enabled` and made typed runtime module registration skip modules explicitly disabled under `modules.<module_id>.enabled = false`. Added a focused test proving `modules.tool/todos.enabled=false` removes `write_todos` from the typed runtime registry. Validation passed: `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_todos_module --all-features`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: extend the module-registration path to another simple typed tool or start splitting sandbox typed tools into capability modules.
- 2026-05-23 15:48 +03: Phase 4c split the broad sandbox typed runtime registration into capability modules: `SandboxExecToolRuntimeModule` (`tool/sandbox-exec`), `SandboxFileOpsToolRuntimeModule` (`tool/sandbox-fileops` for `write_file`, `read_file`, `send_file_to_user`, `list_files`), and `SandboxRecreateToolRuntimeModule` (`tool/sandbox-recreate`). The typed runtime builder no longer registers the whole `SandboxProvider` directly; it registers the sandbox slices through the module path, so `modules.<sandbox-module>.enabled=false` can disable each slice independently. Validation passed: `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_sandbox --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue moving remaining typed runtime providers/modules out of the global executor path, then converge legacy provider registry onto typed modules.
- 2026-05-23 15:56 +03: Phase 4d made legacy `ToolRegistry` honor module enablement for the first migrated tool slices. Added `FilteredToolProvider` for exposing a static module-owned subset of a legacy provider, registered `tool/todos` only when enabled, and split legacy sandbox registration into shared-provider slices for `tool/sandbox-exec`, `tool/sandbox-fileops`, and `tool/sandbox-recreate`. Validation passed: `cargo test -p oxide-agent-core legacy_registry_skips_disabled --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue extracting remaining provider/runtime registrations into module-owned boundaries and reduce global executor registration.
- 2026-05-23 16:01 +03: Phase 4e converged the first migrated slices onto one module-owned registration path. Replaced the typed-only `ToolRuntimeModule` abstraction with `ToolModule`, added shared `ToolModuleContext`, and made each migrated module provide both legacy provider registration and typed runtime executors from the same module struct. The executor now builds one sandbox provider per module context and reuses the same module list for legacy and typed registration. Validation passed: `cargo test -p oxide-agent-core legacy_registry_skips_disabled --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: move another small provider slice, likely compression or delegation, behind the same `ToolModule` path.
- 2026-05-23 16:07 +03: Phase 4f moved `tool/compression` into the shared `ToolModule` path. Added `CompressionToolModule` as a runner-handled legacy-only module, removed unconditional legacy compression registration, and covered `modules.tool/compression.enabled=false` with a registry test while preserving typed runtime behavior where `compress` is not exposed. Validation passed: `cargo test -p oxide-agent-core legacy_registry_skips_disabled --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: move another low-risk direct provider registration behind `ToolModule` or start feature-gating remaining always-compiled provider modules.
- 2026-05-23 16:10 +03: Phase 4g moved main-agent `tool/file-delivery` registration into the shared `ToolModule` path. `ToolModuleContext` now carries `SandboxScope` for modules that need their own sandbox-backed provider, `FileDeliveryToolModule` owns `FileHosterProvider` registration, and the executor no longer registers file hosting directly. Validation passed: `cargo test -p oxide-agent-core legacy_registry_skips_disabled --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue moving direct providers such as `tool/ytdlp` or `tool/delegation` into module-owned registration.
- 2026-05-23 16:13 +03: Phase 4h moved main-agent `tool/ytdlp` registration into the shared `ToolModule` path. Added `YtdlpToolModule`, preserved sandbox scope and progress sender wiring, removed direct `YtdlpProvider` registration from the executor, and covered `modules.tool/ytdlp.enabled=false` with a legacy registry test. Validation passed: `cargo test -p oxide-agent-core legacy_registry_skips_disabled --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: decide whether to move complex `tool/delegation` into module-owned registration or first gate topic/search/provider registrations by module enablement.
- 2026-05-23 16:20 +03: Phase 4i shared `ToolModuleContext` across the legacy registry build and moved `tool/stack-logs` plus `tool/webfetch-md` into module-owned registration. `StackLogsToolModule` and `WebFetchMdToolModule` now own their legacy providers, direct executor registration was removed for those tools, and disabled-module tests cover both `modules.tool/stack-logs.enabled=false` and `modules.tool/webfetch-md.enabled=false`. Validation passed: `cargo test -p oxide-agent-core legacy_registry_skips_disabled --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check -p oxide-agent-core --no-default-features --features tool-webfetch-md`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue reducing direct registration for delegation/media/TTS or introduce module-owned registration for topic-scoped tools.
- 2026-05-23 16:30 +03: Phase 4j moved main-agent `tool/tavily` and `tool/searxng` search registration into the shared `ToolModule` path. `TavilyToolModule` and `SearxngToolModule` now own provider construction and env/config gating, while feature-off warnings remain in the executor. Added disabled-module tests with enabled env config for both search modules plus a regression test that enabled search modules are registered exactly once. Validation passed: `cargo test -p oxide-agent-core legacy_registry --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features tool-tavily`, `cargo check -p oxide-agent-core --no-default-features --features tool-searxng`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: start module-owned registration for context-scoped/topic tools or move the complex `tool/delegation` path.
- 2026-05-23 16:40 +03: Phase 4k moved `tool/tts-kokoro` and `tool/tts-silero` registration into the shared `ToolModule` path. `KokoroTtsToolModule` and `SileroTtsToolModule` now own env config, empty-URL disable behavior, sandbox scope wiring, and progress sender wiring; the executor now calls a module-owned TTS registration section instead of provider-specific constructors. Added disabled-module tests for both TTS modules and a duplicate-registration regression test. Validation passed: `cargo test -p oxide-agent-core legacy_registry --all-features`, `cargo test -p oxide-agent-core main_agent_registry_includes_explicit_media_and_tts_file_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features tool-tts-kokoro`, `cargo check -p oxide-agent-core --no-default-features --features tool-tts-silero`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: move media file analysis or browser-use into module-owned registration, then tackle topic-scoped/manager tools.
- 2026-05-23 16:47 +03: Phase 4l split media file analysis into module-owned registration for `tool/media-audio`, `tool/media-image`, and `tool/media-video`. `ToolModuleContext` now carries the shared `LlmClient`, `MediaAudioToolModule`, `MediaImageToolModule`, and `MediaVideoToolModule` expose one filtered media tool each, and the executor no longer constructs `MediaFileProvider` directly. Added disabled-module tests for all three media modules plus a duplicate-registration regression test. Validation passed: `cargo test -p oxide-agent-core legacy_registry --all-features`, `cargo test -p oxide-agent-core main_agent_registry_includes_explicit_media_and_tts_file_tools --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features tool-media-audio`, `cargo check -p oxide-agent-core --no-default-features --features tool-media-image`, `cargo check -p oxide-agent-core --no-default-features --features tool-media-video`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: move browser-use into module-owned registration or start extracting topic-scoped provider modules.
- 2026-05-23 16:53 +03: Phase 4m moved `tool/browser-use` registration into the shared `ToolModule` path. `ToolModuleContext` now carries shared settings and derived Browser Use profile scope, `BrowserUseToolModule` owns env/config gating plus sandbox/profile wiring, and the executor only delegates browser registration through `register_legacy_tool_module` while preserving feature-off warnings. Added disabled-module coverage and duplicate-registration assertions for browser tools. Validation passed: `cargo test -p oxide-agent-core browser_use --all-features`, `cargo test -p oxide-agent-core legacy_registry --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features tool-browser-use`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: tackle topic-scoped provider modules (`agents_md`, reminders/wiki/manager) or the complex delegation path.
- 2026-05-23 16:59 +03: Phase 4n moved Jira and Mattermost MCP integration registration into module-owned registration. `JiraMcpToolModule` and `MattermostMcpToolModule` use the compiled capability IDs `integration/mcp-jira` and `integration/mcp-mattermost`, own env/config gating and provider construction, and the executor no longer has direct MCP provider constructors for those integrations. Added disabled-module tests for both MCP integrations and a duplicate-registration regression test. Validation passed: `cargo test -p oxide-agent-core legacy_registry --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features integration-mcp-jira`, `cargo check -p oxide-agent-core --no-default-features --features integration-mcp-mattermost`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: move topic-scoped providers (`agents_md`, reminders/wiki/manager) or the complex delegation path into capability modules.
- 2026-05-23 17:14 +03: Phase 4o moved legacy registration for `tool/agents-md` and `tool/reminder` into module-owned registration. `ToolModuleContext` now uses `ToolModuleContextParts` to avoid argument growth and carries optional AGENTS.md and reminder contexts; `AgentsMdToolModule` and `ReminderToolModule` own legacy provider construction, while existing typed runtime provider registration remains direct pending a shared provider-to-runtime adapter. Added disabled-module tests for both topic tools plus a duplicate-registration regression test. Validation passed: `cargo test -p oxide-agent-core legacy_registry --all-features`, `cargo test -p oxide-agent-core agents_md_context_enables_self_editing_tools --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features tool-agents-md`, `cargo check -p oxide-agent-core --no-default-features --features tool-reminder`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: move wiki memory, manager control plane, SSH, or delegation direct registrations; typed runtime adapters remain a known follow-up for full single-source registration.
- 2026-05-23 17:27 +03: Phase 4p moved `tool/wiki-memory` into module-owned legacy and typed runtime registration. Extracted the shared provider-to-runtime adapter into `tool_runtime::provider_executor`, wired `WikiMemoryToolModule` with durable store plus memory scope from `ToolModuleContext`, and reused the adapter for `AgentsMdToolModule` and `ReminderToolModule` typed runtime executors so those topic tools now share the same module source for legacy and typed registration. Added wiki typed-runtime coverage plus disabled-module and duplicate-registration legacy tests. Validation passed: `cargo test -p oxide-agent-core legacy_registry --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_wiki_memory_tools_when_store_configured --all-features`, `cargo test -p oxide-agent-core executor_exposes_wiki_memory_tools_when_store_configured --all-features`, `cargo check -p oxide-agent-core --no-default-features --features tool-wiki-memory`, `cargo check -p oxide-agent-core --no-default-features --features tool-agents-md`, `cargo check -p oxide-agent-core --no-default-features --features tool-reminder`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue reducing direct executor registrations for manager control plane, SSH, and delegation.
- 2026-05-23 17:37 +03: Phase 4q moved `manager/control-plane` into module-owned legacy and typed runtime registration. Added `ManagerControlPlaneModuleContext` for storage/user/lifecycle wiring, `ManagerControlPlaneToolModule` as the single provider construction path, removed direct manager provider creation from the executor registry, and gated executor-level manager tests behind the compiled manager feature. Added disabled-module coverage for both legacy and typed registries plus duplicate-registration coverage for base and lifecycle manager tools. Validation passed: `cargo test -p oxide-agent-core typed_runtime_registry_exposes_manager --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_manager_control_plane_module --all-features`, `cargo test -p oxide-agent-core current_tool_definitions_include_manager_tools_for_v1_route --all-features`, `cargo test -p oxide-agent-core legacy_registry_skips_disabled_manager_control_plane_module --all-features`, `cargo test -p oxide-agent-core legacy_registry_registers_manager_control_plane_module_once --all-features`, `cargo test -p oxide-agent-core manager_ --all-features`, `cargo test -p oxide-agent-core legacy_registry --all-features`, `cargo check -p oxide-agent-core --no-default-features`, `cargo check -p oxide-agent-core --no-default-features --features manager-control-plane`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: move the remaining direct topic/runtime registrations: SSH MCP and delegation.
- 2026-05-23 17:47 +03: Phase 4r moved `integration/ssh-mcp` into module-owned legacy and typed runtime registration. Added `SshMcpModuleContext` for topic infra storage/user/topic/config/approval wiring, `SshMcpToolModule` as the single SSH provider construction path, and removed direct SSH legacy/runtime blocks from the executor registry. Typed runtime coverage now documents the intentional process-only SSH v1 surface (`ssh_exec`, `ssh_sudo_exec`, `ssh_check_process`) while legacy registration still covers file and transfer tools. Validation passed: `cargo test -p oxide-agent-core typed_runtime_registry_exposes_ssh_mcp_tools_when_topic_infra_configured --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_ssh_mcp_module --all-features`, `cargo test -p oxide-agent-core legacy_registry_skips_disabled_ssh_mcp_module --all-features`, `cargo test -p oxide-agent-core legacy_registry_registers_ssh_mcp_module_once --all-features`, `cargo test -p oxide-agent-core legacy_registry --all-features`, `cargo check -p oxide-agent-core --no-default-features --features integration-ssh-mcp`, `cargo check -p oxide-agent-core --no-default-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: move the remaining direct core provider registration for `tool/delegation`.
- 2026-05-23 17:53 +03: Phase 4s moved `tool/delegation` into module-owned legacy registration. Added `DelegationToolModule`, reused existing `ToolModuleContext` data for LLM client, sandbox scope, settings, optional topic AGENTS.md inheritance, and Browser Use profile scope, and removed direct delegation provider construction from the executor registry. Added disabled-module and duplicate-registration tests for delegation tools and tightened feature cfg on AGENTS.md/delegation registry tests. Validation passed: `cargo test -p oxide-agent-core legacy_registry_skips_disabled_delegation_module --all-features`, `cargo test -p oxide-agent-core legacy_registry_registers_delegation_module_once --all-features`, `cargo test -p oxide-agent-core delegation_tool_inherits_agents_md_context_from_executor --all-features`, `cargo check -p oxide-agent-core --no-default-features --features tool-delegation`, `cargo test -p oxide-agent-core legacy_registry --all-features`, `cargo check -p oxide-agent-core --no-default-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: audit executor registry for remaining direct provider construction and then start the next PRD slice outside tool registration.
- 2026-05-23 18:05 +03: Phase 5a started LLM provider modularization. Added `llm::providers::modules` with `LlmProviderModule`, stable provider IDs, module-owned aliases, feature-gated factories, and runtime module-disable handling. `LlmClient::new` no longer contains the hardcoded provider insertion chain; it now consumes `build_configured_providers(settings)`. Validation passed: `cargo test -p oxide-agent-core providers::modules --no-default-features --features llm-opencode-go`, `cargo test -p oxide-agent-core providers::modules --all-features`, `cargo test -p oxide-agent-core llm_client_registers_opencode_go_when_key_present --all-features`, `cargo check -p oxide-agent-core --no-default-features`, provider-specific checks for `llm-opencode-go`, `llm-groq`, and `llm-minimax`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: move provider capability/route validation metadata out of global helper code or split provider module structs into provider-local files.
- 2026-05-23 18:15 +03: Phase 5b moved LLM provider request/media capability metadata into provider modules. `LlmProviderModule` now owns base request capabilities, media capabilities, and model-specific overrides for OpenCode Go, ZAI, and Nvidia; `llm::capabilities` delegates to compiled provider modules instead of a global provider match chain. Added module-ID resolution coverage so OpenCode Go capabilities resolve through module aliases/IDs. Validation passed: `cargo test -p oxide-agent-core llm::capabilities --all-features`, `cargo test -p oxide-agent-core providers::modules --all-features`, `cargo test -p oxide-agent-core llm::capabilities --no-default-features --features llm-opencode-go`, `cargo test -p oxide-agent-core providers::modules --no-default-features --features llm-opencode-go`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: add startup/config validation for model routes that reference non-compiled provider modules.
- 2026-05-23 18:40 +03: Phase 5c removed the direct Google Gemini provider from the project. Deleted the core provider implementation, removed the provider feature/dependency, dropped the vendored SDK and workspace patch, removed direct Gemini credentials from config/docs/examples, and updated Browser Use to accept Gemini-family models only through OpenRouter route inheritance. Validation passed: `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo test -p oxide-agent-core providers::modules --all-features`, `cargo test -p oxide-agent-core llm::capabilities --all-features`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --all-features`, and static search for the removed direct provider identifiers. Attempted `PYTHONPATH=services/browser_use_bridge python3 -m unittest services.browser_use_bridge.tests.test_main`, but the local conda FastAPI/Starlette installation fails before tests with `Router.__init__() got an unexpected keyword argument 'on_startup'`; this is an environment blocker outside the Rust changes. Next: commit this checkpoint, then continue startup/config validation for non-compiled provider modules.
- 2026-05-23 18:47 +03: Phase 5d added startup validation for configured LLM route providers. `AgentSettings::new()` now resolves `CHAT_MODEL_PROVIDER`, agent/sub-agent/media/browser-use/wiki-writer provider overrides, and weighted agent/sub-agent route providers through compiled provider modules, rejecting unknown/non-compiled provider aliases and providers whose module is disabled at runtime. Provider modules expose alias-to-module-ID resolution for config validation. Validation passed: `cargo test -p oxide-agent-core route_provider_validation --all-features`, `cargo test -p oxide-agent-core providers::modules --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: commit this checkpoint, then continue replacing remaining global route/capability assumptions with module-owned config metadata.
- 2026-05-23 18:51 +03: Phase 5e moved route credential detection off hardcoded provider-string predicates. ZAI and OpenCode Go credential checks now resolve configured provider aliases/module IDs through compiled provider modules, so `llm-provider/opencode-go`, `opencode-go`, and `opencode_go` share the same validation path. Added regression coverage for module-ID provider credentials. Validation passed: `cargo test -p oxide-agent-core route_credentials_validation --all-features`, `cargo test -p oxide-agent-core route_provider_validation --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: commit this checkpoint, then continue extracting provider-owned config metadata beyond credential detection.
- 2026-05-23 22:58 +03: Phase 5f moved route credential validation behavior into LLM provider modules. `LlmProviderModule` now has provider-owned `missing_route_config_message`, and `AgentSettings` no longer hardcodes ZAI/OpenCode Go credential predicates; it iterates configured route providers, resolves each compiled provider module once, and delegates missing-config checks to the module. OpenCode Go module tests cover missing/present credential behavior. Validation passed: `cargo test -p oxide-agent-core route_credentials_validation --all-features`, `cargo test -p oxide-agent-core opencode_go_module_owns_missing_route_config_message --all-features`, `cargo test -p oxide-agent-core route_provider_validation --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: commit this checkpoint, then continue extracting provider-owned API endpoint defaults/config schema metadata.
- 2026-05-23 23:10 +03: Phase 5g moved the OpenCode Go provider module implementation into provider-local code. `llm::providers::modules` now only discovers `opencode_go::OpenCodeGoProviderModule`, while aliases, route credential validation, provider factory construction, model-specific structured-output capability, and OpenCode Go model-ID normalization live beside the provider implementation. Validation passed: `cargo test -p oxide-agent-core providers::modules --all-features`, `cargo test -p oxide-agent-core opencode_go_module_owns_missing_route_config_message --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-opencode-go`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: repeat provider-local module extraction for the next LLM provider.
- 2026-05-23 23:25 +03: Phase 5h moved the ZAI provider module implementation into provider-local code. `llm::providers::modules` now only discovers `zai::ZaiProviderModule`, while aliases, route credential validation, provider factory construction, model-specific structured-output capability, and ZAI model-ID normalization live beside the provider implementation. Added provider-module regression tests for missing ZAI route credentials and GLM structured-output capability. Validation passed: `cargo test -p oxide-agent-core providers::modules --all-features`, `cargo test -p oxide-agent-core route_credentials_validation --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-zai`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-zai async-openai claudius aws-sdk-s3 aws-config aws-credential-types aws-types bollard rmcp htmd`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: repeat provider-local module extraction for Nvidia or OpenRouter.
- 2026-05-23 23:39 +03: Phase 5i moved the Nvidia provider module implementation into provider-local code. `llm::providers::modules` now only discovers `nvidia::NvidiaProviderModule`, while aliases, provider factory construction, and model-specific tool/structured-output capability overrides live beside the Nvidia provider implementation and its model capability table. Added provider-module regression coverage for Nvidia model-specific capability overrides. Validation passed: `cargo test -p oxide-agent-core providers::modules --all-features`, `cargo test -p oxide-agent-core llm::capabilities --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-nvidia`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-nvidia zai-rs async-openai claudius aws-sdk-s3 aws-config aws-credential-types aws-types bollard rmcp htmd`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: repeat provider-local module extraction for OpenRouter or the remaining SDK providers.
- 2026-05-23 23:52 +03: Phase 5j moved the OpenRouter provider module implementation into provider-local code. `llm::providers::modules` now only discovers `openrouter::OpenRouterProviderModule`, while aliases, provider factory construction, base request capability metadata, and media capability metadata live beside the OpenRouter provider implementation. Added provider-module regression coverage for OpenRouter media capabilities. Validation passed: `cargo test -p oxide-agent-core providers::modules --all-features`, `cargo test -p oxide-agent-core llm::capabilities --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-openrouter`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-openrouter zai-rs async-openai claudius aws-sdk-s3 aws-config aws-credential-types aws-types bollard rmcp htmd`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: repeat provider-local module extraction for ChatGPT, Groq, Mistral, or MiniMax.
- 2026-05-24 00:05 +03: Phase 5k moved the Groq provider module implementation into provider-local code. `llm::providers::modules` now only discovers `groq::GroqProviderModule`, while aliases, provider factory construction, and base request capability metadata live beside the Groq provider implementation. Added provider-module regression coverage for Groq alias registration and base capabilities. Validation passed: `cargo test -p oxide-agent-core providers::modules --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-groq`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-groq zai-rs claudius aws-sdk-s3 aws-config aws-credential-types aws-types bollard rmcp htmd`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: repeat provider-local module extraction for MiniMax, Mistral, or ChatGPT.
- 2026-05-24 00:16 +03: Phase 5l moved the MiniMax provider module implementation into provider-local code. `llm::providers::modules` now only discovers `minimax::MiniMaxProviderModule`, while aliases, provider factory construction, and base request capability metadata live beside the MiniMax provider implementation. Added provider-module regression coverage for MiniMax alias registration and base capabilities. Validation passed: `cargo test -p oxide-agent-core providers::modules --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-minimax`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-minimax zai-rs async-openai aws-sdk-s3 aws-config aws-credential-types aws-types bollard rmcp htmd`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: repeat provider-local module extraction for Mistral or ChatGPT.
- 2026-05-24 00:28 +03: Phase 5m moved the Mistral provider module implementation into provider-local code. `llm::providers::modules` now only discovers `mistral::MistralProviderModule`, while aliases, provider factory construction, base request capability metadata, and audio media capability metadata live beside the Mistral provider implementation. Added provider-module regression coverage for Mistral alias registration and media capabilities. Validation passed: `cargo test -p oxide-agent-core providers::modules --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-mistral`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-mistral zai-rs claudius aws-sdk-s3 aws-config aws-credential-types aws-types bollard rmcp htmd`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: repeat provider-local module extraction for ChatGPT, then audit `llm::providers::modules` as discovery-only.
- 2026-05-24 00:41 +03: Phase 5n moved the ChatGPT provider module implementation into provider-local code. `llm::providers::modules` now only discovers `chatgpt::ChatGptProviderModule`, while aliases, auth-path resolution, provider factory construction, and base request capability metadata live beside the ChatGPT provider implementation. Added provider-module regression coverage for ChatGPT aliases and base capabilities. Validation passed: `cargo test -p oxide-agent-core providers::modules --all-features`, `cargo check -p oxide-agent-core --no-default-features --features llm-chatgpt`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-chatgpt zai-rs async-openai claudius aws-sdk-s3 aws-config aws-credential-types aws-types bollard rmcp htmd`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: audit the provider module registry for discovery-only ownership and then continue the next PRD modularization slice.
- 2026-05-24 01:08 +03: Phase 7a started storage modularization by introducing `storage::modules` with `StorageBackendModule`, `BuiltStorageBackend`, and `build_primary_storage`. Telegram startup now builds storage through the selected storage module instead of directly calling `R2Storage::new`, and startup maintenance consumes a smaller `PersistedAgentMemoryStore` trait instead of `Arc<R2Storage>`. R2 remains the only durable backend and runtime-disabling `storage/r2` now fails storage construction loudly. Validation passed: `cargo test -p oxide-agent-core storage::modules --no-default-features --features storage-s3-r2`, `cargo check -p oxide-agent-core --no-default-features`, `cargo check -p oxide-agent-core --no-default-features --features storage-s3-r2`, `cargo check -p oxide-agent-transport-telegram --no-default-features --features storage-s3-r2`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --all-features`, and static search found no `R2Storage::new`, `Arc<R2Storage>`, or `R2Storage` references in Telegram runner/startup maintenance. Next: continue storage modularization by moving R2 config under module-owned settings or splitting more storage interfaces.
- 2026-05-24 00:02 +03: Phase 7b moved R2-specific config out of global `AgentSettings`. `ModuleRuntimeConfig` now carries flattened module-local config values, `R2StorageConfig` resolves `modules.storage/r2` endpoint/bucket/credential settings plus `OXIDE_R2_*` module-owned env vars, and `R2Storage::new` accepts `R2StorageConfig` instead of the whole app settings object. Updated R2 integration tests, credential validation, redaction patterns, `.env.example`, and README snippets to use `OXIDE_R2_*`; static search found no remaining global `r2_*` `AgentSettings` fields, old `R2_*` env names, or `R2Storage::new(settings)` paths in the affected code. Validation passed: `cargo test -p oxide-agent-core storage::r2_config --no-default-features --features storage-s3-r2`, `cargo test -p oxide-agent-core storage::modules --no-default-features --features storage-s3-r2`, `cargo test -p oxide-agent-core test_config_env_loading --all-features`, `cargo test -p oxide-agent-core module_runtime_settings_deserialize_enabled_flags --all-features`, `cargo check -p oxide-agent-core --no-default-features`, `cargo check -p oxide-agent-core --no-default-features --features storage-s3-r2`, `cargo check -p oxide-agent-transport-telegram --no-default-features --features storage-s3-r2`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue storage modularization by moving remaining storage startup/schema concerns into module metadata or start the sandbox backend/tool split.
- 2026-05-24 00:18 +03: Phase 8a started sandbox backend modularization by separating the sandboxd client backend from Docker direct. `sandbox-backend-sandboxd-client` now selects only the broker protocol instead of pulling `sandbox-backend-docker-direct`; `sandbox-daemon` selects Docker direct for the deployable broker service. `SandboxManager` now compiles Docker manager/server code only under `sandbox-backend-docker-direct`, compiles broker-client state only under `sandbox-backend-sandboxd-client`, and selects the only compiled backend without requiring `SANDBOX_BACKEND` unless both backends are compiled. `SandboxBrokerServer` and Docker-backed broker request handlers are Docker-gated while broker protocol/client types remain client-only buildable. Added a leakage gate for `sandbox-backend-sandboxd-client`. Validation passed: `cargo test -p oxide-agent-core sandbox::broker --no-default-features --features sandbox-backend-sandboxd-client`, `cargo test -p oxide-agent-core sandbox::manager --no-default-features --features sandbox-backend-docker-direct`, `cargo check -p oxide-agent-core --no-default-features --features sandbox-backend-sandboxd-client`, `cargo check -p oxide-agent-core --no-default-features --features sandbox-backend-docker-direct`, `cargo check -p oxide-agent-core --no-default-features --features sandbox-backend-docker-direct,sandbox-backend-sandboxd-client`, `cargo check -p oxide-agent-sandboxd --no-default-features --features profile-full`, `cargo check --workspace`, `cargo check --workspace --no-default-features --features profile-full`, `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 8 by introducing smaller sandbox backend capability traits or moving sandbox tool modules to require backend capabilities explicitly.
- 2026-05-24 10:28 +03: Phase 8b made capability requirements machine-enforced for sandbox tool/backend topology. `CapabilityRequirement` now supports single-capability and any-of requirements; compiled manifests fail when a declared dependency has no compiled provider, and enabled manifests fail when runtime config disables the provider capability that an enabled module needs. Sandbox backend modules now export trait-like capabilities for `fileops`, `exec`, `lifecycle`, and `diagnostics`; `tool/sandbox-fileops`, `tool/sandbox-exec`, `tool/sandbox-recreate`, `tool/stack-logs`, and `sandbox-daemon/sandboxd` declare explicit requirements. Added regression coverage for missing compiled requirements, disabled runtime requirements, and the compiled sandbox exec backend requirement. Validation passed: `cargo test -p oxide-agent-core capabilities::manifest --all-features`, `cargo test -p oxide-agent-core capabilities::manifest --no-default-features --features tool-sandbox-exec,sandbox-backend-sandboxd-client`, `cargo check -p oxide-agent-core --no-default-features --features tool-sandbox-exec,sandbox-backend-sandboxd-client`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 8 by replacing the broad `SandboxProvider`/`SandboxManager` coupling with smaller backend capability traits used by each sandbox tool module.
- 2026-05-24 10:40 +03: Phase 8c reduced main-agent sandbox tool coupling by extracting `SandboxRuntime` as shared sandbox state and adding narrow `SandboxExecProvider`, `SandboxFileOpsProvider`, and `SandboxLifecycleProvider` slices. `ToolModuleContext` now carries the shared runtime instead of a broad `SandboxProvider`, and `SandboxExecToolModule`, `SandboxFileOpsToolModule`, and `SandboxRecreateToolModule` register their own provider/executor slices directly rather than filtering a broad provider. The broad `SandboxProvider` remains as a compatibility aggregate for existing sub-agent/delegation paths while those are migrated. Validation passed: `cargo test -p oxide-agent-core providers::sandbox --all-features`, `cargo test -p oxide-agent-core legacy_registry_skips_disabled --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features tool-sandbox-exec,sandbox-backend-sandboxd-client`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Note: one attempted combined `cargo test` command was invalid because Cargo accepts a single test filter; both filters passed when run separately. Next: continue Phase 8 by migrating sub-agent/delegation sandbox registration off the broad aggregate or by introducing backend traits under `SandboxRuntime`.
- 2026-05-24 10:50 +03: Phase 8d migrated sub-agent/delegation sandbox registration off the broad sandbox aggregate. `DelegationProvider` now builds sub-agent sandbox tools from `SandboxExecProvider` and a delivery-free `SandboxFileOpsProvider` slice only when the matching sandbox tool features are compiled; it no longer registers `SandboxProvider` or `FileHosterProvider` for sub-agents. `upload_file` is now blocked for sub-agents alongside `send_file_to_user`, and raw sub-agent providers no longer expose file delivery or `recreate_sandbox`. `SandboxFileOpsProvider` gained a `without_delivery` constructor so main-agent fileops can keep the legacy delivery tool while sub-agents get only `write_file`, `read_file`, and `list_files`. Static search confirms production `SandboxProvider` usage is now confined to the sandbox module compatibility aggregate/export. Validation passed: `cargo test -p oxide-agent-core providers::sandbox --all-features`, `cargo test -p oxide-agent-core build_sub_agent_providers --all-features`, `cargo test -p oxide-agent-core sub_agent_blocklist_includes_sensitive_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features tool-delegation`, `cargo check -p oxide-agent-core --no-default-features --features tool-delegation,tool-sandbox-exec,sandbox-backend-sandboxd-client`, `cargo check -p oxide-agent-core --no-default-features --features tool-delegation,tool-sandbox-fileops,sandbox-backend-sandboxd-client`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 8 by introducing backend traits under `SandboxRuntime`/`SandboxManager` or by splitting file delivery out of the main-agent `tool/sandbox-fileops` surface.
- 2026-05-24 11:08 +03: Phase 8e split main-agent file delivery out of `tool/sandbox-fileops`. `SandboxProvider` and `SandboxFileOpsProvider` no longer define or execute `send_file_to_user`; sandbox fileops now exposes only `write_file`, `read_file`, and `list_files`. `FileHosterProvider` now owns both `send_file_to_user` and `upload_file`, uses the shared `SandboxRuntime`, and is registered by `FileDeliveryToolModule` for both legacy and typed runtime paths. Registry tests now prove disabling `tool/sandbox-fileops` leaves delivery available, while disabling `tool/file-delivery` removes both delivery tools without removing sandbox fileops. Validation passed: `cargo test -p oxide-agent-core providers::filehoster --all-features`, `cargo test -p oxide-agent-core providers::sandbox --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_sandbox_fileops_module --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_file_delivery_module --all-features`, `cargo test -p oxide-agent-core legacy_registry_skips_disabled_sandbox_fileops_module --all-features`, `cargo test -p oxide-agent-core legacy_registry_skips_disabled_file_delivery_module --all-features`, `cargo test -p oxide-agent-core build_sub_agent_providers --all-features`, `cargo test -p oxide-agent-core sub_agent_blocklist_includes_sensitive_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features tool-file-delivery`, `cargo check -p oxide-agent-core --no-default-features --features tool-file-delivery,sandbox-backend-sandboxd-client`, `cargo check -p oxide-agent-core --no-default-features --features tool-file-delivery,tool-sandbox-fileops,sandbox-backend-sandboxd-client`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 8 by deleting the broad sandbox compatibility aggregate or extracting backend traits under `SandboxRuntime`/`SandboxManager`.
- 2026-05-24 11:16 +03: Phase 8f deleted the public broad `SandboxProvider` compatibility aggregate. The sandbox module now exports only `SandboxExecProvider`, `SandboxFileOpsProvider`, `SandboxLifecycleProvider`, and `SandboxRuntime`; shared execution logic moved behind private `SandboxToolHandlers` instead of a mixed-surface provider. Static search confirms `SandboxProvider`, `clone_with_progress_tx`, and `SANDBOX_ALL_TOOL_NAMES` are gone from `crates/oxide-agent-core/src`. Validation passed: `cargo test -p oxide-agent-core providers::sandbox --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo check -p oxide-agent-core --no-default-features --features tool-sandbox-exec,sandbox-backend-sandboxd-client`, `cargo check -p oxide-agent-core --no-default-features --features tool-sandbox-fileops,sandbox-backend-sandboxd-client`, `cargo check -p oxide-agent-core --no-default-features --features tool-sandbox-recreate,sandbox-backend-sandboxd-client`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 8 by extracting explicit sandbox backend traits from `SandboxRuntime`/`SandboxManager` or moving to transport/profile bootstrap work.
- 2026-05-24 11:31 +03: Phase 8g introduced explicit narrow sandbox backend traits and moved sandbox tool providers onto trait objects. Added `sandbox::traits` with `SandboxBackend`, `SandboxExec`, `SandboxFileOps`, `SandboxLifecycle`, `SandboxCapability`, `SandboxBackendId`, and `SandboxFileListing`; `SandboxRuntime` implements those traits as the current scope-bound backend facade. `SandboxExecProvider`, `SandboxFileOpsProvider`, and `SandboxLifecycleProvider` now store `Arc<dyn SandboxExec>`, `Arc<dyn SandboxFileOps>`, and `Arc<dyn SandboxLifecycle>` respectively, and typed runtime executors dispatch through the matching trait surface instead of the full runtime/manager surface. Validation passed: `cargo test -p oxide-agent-core providers::sandbox --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo test -p oxide-agent-core sandbox_runtime_exposes_narrow_backend_capabilities --all-features`, `cargo check -p oxide-agent-core --no-default-features --features tool-sandbox-exec,sandbox-backend-sandboxd-client`, `cargo check -p oxide-agent-core --no-default-features --features tool-sandbox-fileops,sandbox-backend-sandboxd-client`, `cargo check -p oxide-agent-core --no-default-features --features tool-sandbox-recreate,sandbox-backend-sandboxd-client`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue replacing concrete `SandboxManager` usage in file delivery/media/manager paths with the new narrow traits or move to transport/profile bootstrap.
- 2026-05-24 11:44 +03: Phase 8h moved `tool/file-delivery` off direct `SandboxManager` access and onto the narrow sandbox trait surfaces. `FileHosterProvider` now stores `Arc<dyn SandboxFileOps>` for chat delivery and `Arc<dyn SandboxExec>` for GoFile upload, `SandboxFileOps` exposes file metadata via `file_size_bytes`, and the shared path resolver module is gated to media features because file delivery no longer depends on it. `send_file_to_user` now resolves paths through fileops metadata and reads through `SandboxFileOps`, while `upload_file` keeps shell-based remote upload through `SandboxExec`. Validation passed: `cargo check -p oxide-agent-core --no-default-features --features tool-file-delivery`, `cargo check -p oxide-agent-core --no-default-features --features tool-file-delivery,sandbox-backend-sandboxd-client`, `cargo test -p oxide-agent-core providers::filehoster --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_file_delivery_module --all-features`, `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`, `cargo test -p oxide-agent-core sandbox_runtime_exposes_narrow_backend_capabilities --all-features`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: inspect remaining concrete sandbox/transport bootstrap couplings and continue the next PRD-aligned modularization slice.
- 2026-05-24 11:51 +03: Phase 8i moved explicit media file analysis off direct `SandboxManager` access. `MediaFileProvider` now receives `Arc<dyn SandboxFileOps>` for media reads/metadata and `Arc<dyn SandboxExec>` for path probing, remote URL downloads, and cleanup; the media `ToolModule` builds it from the shared `SandboxRuntime` instead of constructing a separate sandbox manager. The shared media path resolver now depends on `SandboxExec` rather than `SandboxManager`, and static search confirms `media_file.rs`/`path.rs` no longer import `SandboxManager`. Validation passed: `cargo check -p oxide-agent-core --no-default-features --features tool-media-audio`, `cargo check -p oxide-agent-core --no-default-features --features tool-media-image`, `cargo check -p oxide-agent-core --no-default-features --features tool-media-video`, `cargo test -p oxide-agent-core providers::media_file --all-features`, `cargo test -p oxide-agent-core main_agent_registry_includes_explicit_media_and_tts_file_tools --all-features`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 8 by migrating TTS, yt-dlp, Browser Use, manager sandbox admin, session/preprocessor, or stack diagnostics off concrete sandbox manager paths as appropriate.
- 2026-05-24 12:00 +03: Phase 8j moved Kokoro and Silero TTS file-output workflows off direct `SandboxManager` access. Both TTS providers now keep optional narrow `SandboxFileOps`/`SandboxExec` trait objects for sandbox file writes and directory creation, while progress-channel voice delivery remains unchanged. TTS tool modules pass the shared `SandboxRuntime` into providers instead of a raw sandbox scope, and the env-dependent TTS client default tests now use the existing test env mutex so registry tests cannot race `KOKORO_TTS_URL`/`SILERO_TTS_URL`. Static search confirms TTS provider paths no longer contain `SandboxManager`, `ensure_sandbox`, or direct `exec_command` usage. Validation passed: `cargo check -p oxide-agent-core --no-default-features --features tool-tts-kokoro`, `cargo check -p oxide-agent-core --no-default-features --features tool-tts-silero`, `cargo test -p oxide-agent-core tts --all-features`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 8 by migrating yt-dlp, Browser Use, manager sandbox admin, session/preprocessor, or stack diagnostics off concrete sandbox manager paths as appropriate.
- 2026-05-24 12:09 +03: Phase 8k introduced the narrow `SandboxDiagnostics` trait and moved `tool/stack-logs` behind it. Added `sandbox::diagnostics::SandboxDiagnosticsRuntime` as the concrete diagnostics facade that delegates to the compiled sandbox diagnostics backend, while `StackLogsProvider` now stores `Arc<dyn SandboxDiagnostics>` and no longer imports `SandboxManager` directly. This aligns the stack log tool with the PRD `SandboxDiagnostics` split while preserving existing Docker/broker backend selection. Validation passed: `cargo check -p oxide-agent-core --no-default-features --features tool-stack-logs`, `cargo test -p oxide-agent-core providers::stack_logs --all-features`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 8 by migrating yt-dlp, Browser Use, manager sandbox admin, session/preprocessor, or remaining sandbox facade boundaries.
- 2026-05-24 12:18 +03: Phase 8l moved `tool/ytdlp` off direct `SandboxManager` access and onto `SandboxExec`/`SandboxFileOps`. `YtdlpProvider` now stores narrow trait objects, creates the downloads directory through `SandboxExec`, runs cleanup through the exec trait, reads delivered files through `SandboxFileOps`, and uses `file_size_bytes` for downloaded media size metadata. The main ytdlp tool module now passes the shared `SandboxRuntime`, and the compiled capability manifest now declares that `tool/ytdlp` requires both sandbox exec and sandbox fileops backend capabilities. Added manifest regression coverage for the ytdlp backend requirements. Validation passed: `cargo check -p oxide-agent-core --no-default-features --features tool-ytdlp`, `cargo check -p oxide-agent-core --no-default-features --features tool-ytdlp,sandbox-backend-sandboxd-client`, `cargo test -p oxide-agent-core legacy_registry_skips_disabled_ytdlp_module --all-features`, `cargo test -p oxide-agent-core capabilities::manifest --all-features`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 8 by migrating Browser Use, manager sandbox admin, session/preprocessor, or remaining sandbox facade boundaries.
- 2026-05-24 12:26 +03: Phase 8m moved Browser Use artifact hydration off direct `SandboxManager` access. `BrowserUseProvider` now has optional `SandboxFileOps`/`SandboxExec` backends, writes screenshots/artifacts through fileops, creates artifact directories through exec, and preserves no-op hydration when no sandbox backend is attached. The Browser Use tool module now passes the shared `SandboxRuntime` instead of a raw scope; `with_sandbox_scope` remains as a runtime-building compatibility constructor for delegation paths. Static search confirms the Browser Use provider no longer imports `SandboxManager`, stores `Mutex<Option<SandboxManager>>`, or calls `exec_command` directly. Validation passed: `cargo check -p oxide-agent-core --no-default-features --features tool-browser-use`, `cargo test -p oxide-agent-core browser_use --all-features`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 8 by migrating manager sandbox admin, session/preprocessor, or remaining sandbox facade boundaries.
- 2026-05-24 12:40 +03: Phase 8n moved manager control-plane sandbox administration off direct `SandboxManager` access. Added the narrow `SandboxAdmin` trait and `SandboxAdminRuntime` facade for user/topic sandbox inventory and lifecycle operations, then changed default manager sandbox cleanup/control adapters to depend on `Arc<dyn SandboxAdmin>` while preserving existing manager test overrides. `SandboxAdminRuntime` stays available in no-default builds through `manager_stub`, so always-compiled manager provider code remains buildable even when `manager-control-plane` is not selected. Static search confirms `crates/oxide-agent-core/src/agent/providers/manager_control_plane` no longer contains `SandboxManager` or `DockerTopicSandbox`. Validation passed: `cargo check -p oxide-agent-core --no-default-features`, `cargo check -p oxide-agent-core --no-default-features --features manager-control-plane`, `cargo check -p oxide-agent-core --no-default-features --features manager-control-plane,sandbox-backend-sandboxd-client`, `cargo check -p oxide-agent-core --no-default-features --features manager-control-plane,sandbox-backend-docker-direct`, `cargo test -p oxide-agent-core sandbox::admin --no-default-features`, `cargo test -p oxide-agent-core providers::manager_control_plane::tests::sandboxes --all-features`, `cargo test -p oxide-agent-core providers::manager_control_plane --all-features`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 8 by migrating session/preprocessor sandbox lifecycle usage or finalizing remaining sandbox facade boundaries.
- 2026-05-24 12:51 +03: Phase 8o removed the remaining direct `SandboxManager` coupling from agent session lifecycle and input preprocessing. `AgentSession` no longer stores a `SandboxManager` or exposes manager references; force-recreate and destroy now use the narrow `SandboxAdmin` facade by scope. `Preprocessor` now builds document-upload handling from `SandboxRuntime` as `SandboxExec` plus `SandboxFileOps`, uses exec only for the upload-size probe, and writes uploaded documents through fileops. Added preprocessor tests with fake narrow sandbox backends to prove document upload no longer needs a concrete manager. Static search confirms `agent/session.rs` and `agent/preprocessor.rs` no longer contain `SandboxManager`, `ensure_sandbox`, `sandbox_mut`, `create_sandbox`, direct `exec_command`, `get_uploads_size`, or `upload_file` calls. Validation passed: `cargo test -p oxide-agent-core agent::preprocessor --all-features`, `cargo test -p oxide-agent-core agent::session --all-features`, `cargo check -p oxide-agent-core --no-default-features`, `cargo check --workspace`, all six profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`), `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: inspect remaining direct `SandboxManager` usage; expected legitimate uses should now be constrained to sandbox facade/backend modules.
- 2026-05-24 12:54 +03: Phase 8p added a static regression guard for the sandbox manager boundary. `tool_runtime_static_guards.rs` now recursively scans `src/agent` and fails if `SandboxManager` appears outside the explicitly allowed sandbox tool facade (`src/agent/providers/sandbox.rs`), preventing future provider/session/preprocessor slices from reintroducing concrete manager access. Validation passed: `cargo test -p oxide-agent-core sandbox_manager_usage_stays_inside_sandbox_facades --all-features`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: move to the next PRD milestone slice, likely transport/profile-aware bootstrap or Docker/Compose profile selection, unless another sandbox facade cleanup is needed.
- 2026-05-24 13:06 +03: Phase 10a started Docker/Compose profile selection. Added package-level app profile features on `oxide-agent-telegram-bot` so Docker builds can select `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, or `profile-full` through one binary-crate feature. Added `docker/Dockerfile.app`, a profile-aware app Dockerfile that builds with `--no-default-features`, takes explicit `CARGO_FEATURES`/`PACKAGES`/`BINARIES`, and copies only selected compiled binaries into the runtime image. Added `docker/compose.embedded-opencode-local.yml` with only the Telegram app service and no sandboxd, searxng, browser-use, Docker socket, or sandbox volumes. Added `docker/compose.full.yml` as the full profile composition with `oxide_agent`, `sandboxd`, `sandbox_image`, and `searxng`. Added `scripts/check-compose-profile.sh` to make compose service absence/presence rules CI-ready. Validation passed: `docker compose -f docker/compose.embedded-opencode-local.yml config --services`, `docker compose -f docker/compose.full.yml config --services`, `docker compose -f docker/compose.embedded-opencode-local.yml config -q`, `docker compose -f docker/compose.full.yml config -q`, `scripts/check-compose-profile.sh embedded-opencode-local`, `scripts/check-compose-profile.sh full`, `docker build --check -f docker/Dockerfile.app --build-arg CARGO_FEATURES=oxide-agent-telegram-bot/profile-embedded-opencode-local --build-arg PACKAGES=oxide-agent-telegram-bot --build-arg BINARIES=oxide-agent-telegram-bot .`, `docker build --check -f docker/Dockerfile.app --build-arg CARGO_FEATURES=oxide-agent-telegram-bot/profile-full,oxide-agent-sandboxd/profile-full --build-arg PACKAGES='oxide-agent-telegram-bot oxide-agent-sandboxd' --build-arg BINARIES='oxide-agent-telegram-bot oxide-agent-sandboxd' .`, `cargo check -p oxide-agent-telegram-bot --no-default-features --features profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, and `profile-full`, `cargo check -p oxide-agent-sandboxd --no-default-features --features profile-full`, `cargo check --workspace`, all six workspace profile checks, `scripts/check-cargo-tree-deny.sh sandbox-backend-sandboxd-client`, `scripts/check-cargo-tree-deny.sh profile-no-sandbox`, `scripts/check-cargo-tree-deny.sh llm-opencode-go`, `bash -n scripts/check-compose-profile.sh`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 10 by adding profile-specific compose files for media/search/dev or moving MCP/browser-use binary/service requirements behind module-specific Docker stages.
- 2026-05-24 13:09 +03: Phase 10b completed the initial PRD compose profile file set. Added `docker/compose.search.yml`, `docker/compose.media.yml`, and `docker/compose.dev.yml`; search and media profiles intentionally contain only `oxide_agent` because their current Cargo profiles do not select internal `tool-searxng`, browser-use, sandboxd, or Docker-backed sandbox services, while dev mirrors the full local stack with sandboxd, sandbox image, and searxng. Extended `scripts/check-compose-profile.sh` to validate all five required profile files (`embedded-opencode-local`, `search`, `media`, `dev`, `full`). Validation passed: `docker compose -f docker/compose.search.yml config -q`, `docker compose -f docker/compose.media.yml config -q`, `docker compose -f docker/compose.dev.yml config -q`, `scripts/check-compose-profile.sh embedded-opencode-local`, `search`, `media`, `dev`, and `full`, `bash -n scripts/check-compose-profile.sh`, `cargo fmt --all --check`, and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 10 by moving MCP/browser-use binary/service requirements behind module-specific Docker stages or adding CI workflow coverage for Docker/Compose topology.
- 2026-05-24 13:13 +03: Phase 10c added CI coverage for the new Docker/Compose profile topology. `.github/workflows/modular-architecture.yml` now checks all five compose profile files with `scripts/check-compose-profile.sh` and runs `docker build --check` for the embedded and full `docker/Dockerfile.app` argument sets. Validation passed: `scripts/check-compose-profile.sh embedded-opencode-local`, `search`, `media`, `dev`, and `full`; `docker build --check -f docker/Dockerfile.app --build-arg CARGO_FEATURES=oxide-agent-telegram-bot/profile-embedded-opencode-local --build-arg PACKAGES=oxide-agent-telegram-bot --build-arg BINARIES=oxide-agent-telegram-bot .`; `docker build --check -f docker/Dockerfile.app --build-arg CARGO_FEATURES=oxide-agent-telegram-bot/profile-full,oxide-agent-sandboxd/profile-full --build-arg PACKAGES='oxide-agent-telegram-bot oxide-agent-sandboxd' --build-arg BINARIES='oxide-agent-telegram-bot oxide-agent-sandboxd' .`; Python YAML parse for `.github/workflows/modular-architecture.yml`; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. `actionlint` was not available in the local environment. Next: continue Phase 10 by moving MCP/browser-use binary/service requirements behind module-specific Docker stages or start adding size/manifest snapshot gates from the final PRD acceptance matrix.
- 2026-05-24 13:22 +03: Phase 11a added the first compiled capability manifest CI guard. Added `scripts/check-compiled-capabilities.sh`, which runs `oxide-agent-telegram-bot capabilities --compiled --json` for a selected app profile, verifies deterministic module/capability ordering, rejects duplicate IDs, enforces required modules/capabilities, forbids profile-incompatible module/capability prefixes, and keeps direct Google Gemini provider IDs absent. The modular architecture workflow now runs this guard for `embedded-opencode-local`, `search-only`, `media-enabled`, and `full`. Validation passed: `scripts/check-compiled-capabilities.sh embedded-opencode-local`, `search-only`, `media-enabled`, and `full`; `bash -n scripts/check-compiled-capabilities.sh`; Python YAML parse for `.github/workflows/modular-architecture.yml`; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: add binary/image size budget checks or continue deleting legacy typed/legacy registration paths.
- 2026-05-24 13:25 +03: Phase 11b removed remaining direct Google Gemini and legacy skills environment surface from deployment examples. `.env.example` and `.github/workflows/ci-cd.yml` no longer declare, forward, or write `GEMINI_API_KEY`, and the old `SKILL_TOKEN_BUDGET` deploy variable was removed from the workflow after the skills/embeddings subsystem deletion. Gemini-family model IDs remain only with OpenRouter provider settings. Validation passed: static search for `GEMINI_API_KEY` and `SKILL_TOKEN_BUDGET` across workflow/env/docs/config/code returned no matches; Python YAML parse for `.github/workflows/ci-cd.yml` and `.github/workflows/modular-architecture.yml`; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: add binary/image size budget checks or continue deleting legacy typed/legacy registration paths.
- 2026-05-24 13:33 +03: Phase 11c removed runtime loaded-skill accounting after the skills/embeddings subsystem deletion. `AgentSession`, `EphemeralSession`, `AgentContext`, compaction budget estimates, token snapshots, runner route projections, runtime test snapshots, and Telegram progress rendering no longer carry `loaded_skill_tokens`, `skill_token_count`, `register_loaded_skill`, `is_skill_loaded`, or `clear_loaded_skills`. Progress UI now reports `flow | prompt | tools` without the permanently-zero skills column. Validation passed: `cargo check -p oxide-agent-core --all-features`; `cargo test -p oxide-agent-core compaction::budget --all-features`; `cargo test -p oxide-agent-transport-telegram progress_render --all-features`; `cargo check --workspace`; all six workspace profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`); static search for removed loaded-skill accounting identifiers in core/runtime/telegram returned no matches; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: remove historical `SkillContext` message kind/config-topic skill remnants or add binary/image size budget checks.
- 2026-05-24 13:36 +03: Phase 11d removed the topic-level `skills` config surface. `TelegramTopicSettings` no longer accepts or stores a `skills` whitelist, topic route test fixtures no longer construct it, `.env.example` no longer documents `topicConfigs[].skills`, and AGENTS/README crate descriptions no longer point at removed `agent/skills` or root `skills/` directories. Validation passed: `cargo test -p oxide-agent-transport-telegram config --all-features`; `cargo test -p oxide-agent-transport-telegram topic_route --all-features`; static search for topic `skills` config in telegram source/tests and `.env.example` returned no matches; `cargo check --workspace`; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: remove historical `SkillContext` message kind or add binary/image size budget checks.
- 2026-05-24 13:42 +03: Phase 11e removed the historical `SkillContext` memory kind. Deleted `AgentMessageKind::SkillContext`, `AgentMessage::skill_context`, `HotMemoryBudget::skill_context_tokens`, pinned-history handling for skill context, and the associated compaction/memory assertions. This completes the code-side removal of the old skills/embeddings RAG surface; remaining `skills` mentions are only documentation statements that the legacy subsystem was removed or that old `skills/AGENT.md` is not used. Validation passed: `cargo test -p oxide-agent-core agent::memory::tests::test_message_kinds_capture_compaction_intent --all-features`; `cargo test -p oxide-agent-core compaction --all-features`; static search for `SkillContext`, `skill_context_tokens`, `skill_context`, and `Loaded skill` in core returned no matches; `cargo check --workspace`; all six workspace profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`); `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: add binary/image size budget checks or continue deleting legacy typed/legacy registration paths.
- 2026-05-24 14:01 +03: Phase 11f added binary and Docker image size budget gates for the primary modular profiles. Added `scripts/check-profile-size-budget.sh` to build selected release binaries and/or profile-aware app images, enforce per-profile budgets, and print explicit env-var override names only when accepting an intentional new baseline. `.github/workflows/modular-architecture.yml` now runs the budget guard for `embedded-opencode-local` and `full` in both `binary` and `image` modes. Initial local baselines passed: embedded bot binary 46.8 MiB <= 66.8 MiB; full bot binary 60.3 MiB <= 85.8 MiB; full sandboxd binary 10.6 MiB <= 19.1 MiB; embedded image 172.1 MiB uncompressed and 61.2 MiB compressed <= 429.2/209.8 MiB; full image 219.6 MiB uncompressed and 77.2 MiB compressed <= 619.9/305.2 MiB. Validation passed: `bash -n scripts/check-profile-size-budget.sh`; Python YAML parse for `.github/workflows/modular-architecture.yml`; `scripts/check-profile-size-budget.sh embedded-opencode-local binary`; `scripts/check-profile-size-budget.sh full binary`; `scripts/check-profile-size-budget.sh embedded-opencode-local image`; `scripts/check-profile-size-budget.sh full image`; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue deleting legacy typed/legacy registration paths or add remaining final acceptance guards.
- 2026-05-24 14:08 +03: Phase 11g moved `tool/wiki-memory` off the generic legacy-to-typed adapter. `WikiMemoryProvider` now exposes native typed runtime executors with shared execution serialization and direct dispatch to list/read/delete handlers, while `WikiMemoryToolModule` no longer calls `provider_runtime_executors` for wiki memory. Added a typed runtime executor test that reads a wiki page through the native executor. Validation passed: `cargo test -p oxide-agent-core providers::wiki_memory --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_wiki_memory_tools_when_store_configured --all-features`; static search confirms `WikiMemoryToolModule` no longer references `provider_runtime_executors`; `cargo check -p oxide-agent-core --no-default-features --features tool-wiki-memory`; `cargo check --workspace`; all six workspace profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`); `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue removing `provider_runtime_executors` usage from `agents_md`, `manager/control-plane`, and `reminder`, then delete the adapter.
- 2026-05-24 14:12 +03: Phase 11h moved `tool/agents-md` off the generic legacy-to-typed adapter. `AgentsMdProvider` now exposes native typed runtime executors for `agents_md_get` and `agents_md_update`, shares a serialized execution lock across both tools, and dispatches through its own internal handlers instead of the adapter. `AgentsMdToolModule` now builds those native executors directly. Added a typed runtime executor test that reads topic AGENTS.md through `agents_md_get`. Validation passed: `cargo test -p oxide-agent-core providers::agents_md --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_manager_tools_when_manager_enabled --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_wiki_memory_tools_when_store_configured --all-features`; static search confirms `AgentsMdToolModule` no longer references `provider_runtime_executors`; `cargo check -p oxide-agent-core --no-default-features --features tool-agents-md`; `cargo check --workspace`; all six workspace profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`); `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue removing `provider_runtime_executors` usage from `manager/control-plane` and `reminder`, then delete the adapter.
- 2026-05-24 14:16 +03: Phase 11i moved `tool/reminder` off the generic legacy-to-typed adapter. `ReminderProvider` now owns native typed runtime executors for schedule/list/cancel/pause/resume/retry, shares a serialized execution lock across reminder tools, and dispatches through its own internal handlers instead of `provider_runtime_executors`. `ReminderToolModule` now builds those native executors directly. Added a typed runtime executor test for `reminder_list` using no-op storage. Validation passed: `cargo test -p oxide-agent-core providers::reminder --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_manager_tools_when_manager_enabled --all-features`; static search confirms only `manager/control-plane` still calls `provider_runtime_executors`; `cargo check -p oxide-agent-core --no-default-features --features tool-reminder`; `cargo check --workspace`; all six workspace profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`); `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: move `manager/control-plane` off `provider_runtime_executors`, then delete the adapter module/export.
- 2026-05-24 14:21 +03: Phase 11j removed the generic legacy-to-typed tool adapter. `ManagerControlPlaneProvider` now owns native typed runtime executors for its control-plane tool surface, shares a serialized execution lock, and dispatches through its own internal handlers. `ManagerControlPlaneToolModule` now builds those native executors directly. Deleted `agent/tool_runtime/provider_executor.rs` and removed the module/export from `agent/tool_runtime/mod.rs`; static search for `provider_runtime_executors`, `ProviderRuntimeExecutor`, and `provider_executor` now returns no source matches. Added a typed runtime execution test for `agent_profile_get`. Validation passed: `cargo test -p oxide-agent-core providers::manager_control_plane::tests::registry --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_manager_tools_when_manager_enabled --all-features`; `cargo check -p oxide-agent-core --no-default-features --features manager-control-plane`; `cargo check --workspace`; all six workspace profile checks (`profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`, `profile-full`); `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue collapsing the remaining legacy `ToolRegistry`/`build_tool_registry` execution path toward the typed runtime as the only tool registry.
- 2026-05-24 14:30 +03: Phase 11k moved `tool/stack-logs` onto native typed executors without reintroducing the deleted generic adapter. `StackLogsProvider` now owns typed executors for `stack_logs_list_sources` and `stack_logs_fetch`, shares a serialized execution lock, and keeps legacy `ToolProvider` dispatch as the temporary fallback path. `StackLogsToolModule` now registers these native executors directly. Added a fake diagnostics backend test proving typed runtime execution for `stack_logs_list_sources`. Validation passed: `cargo test -p oxide-agent-core providers::stack_logs --all-features`; `cargo check -p oxide-agent-core --no-default-features --features tool-stack-logs`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue native typed executor migration for remaining legacy-only modules such as `webfetch_md`, search, TTS/media, MCP, browser-use, delegation, and compression before deleting legacy `ToolRegistry`.
- 2026-05-24 14:36 +03: Phase 11l moved `tool/webfetch-md` onto a native typed executor. `WebFetchMdProvider` now owns the `web_markdown` typed executor, reuses the existing fetch path with the runtime cancellation token, classifies malformed JSON arguments as typed invalid-argument errors, and `WebFetchMdToolModule` registers the executor directly. Added a no-external-network typed execution test using a local HTTP server plus reqwest DNS override, and added typed registry coverage for both exposed and disabled `tool/webfetch-md` module states. Validation passed: `cargo test -p oxide-agent-core providers::webfetch_md --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_webfetch_tool --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_webfetch_module --all-features`; `cargo check -p oxide-agent-core --no-default-features --features tool-webfetch-md`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); static search confirms the deleted generic adapter symbols remain absent; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue native typed executor migration for search/TTS/media/MCP/browser-use/delegation modules, then delete the remaining legacy `ToolRegistry` execution path.
- 2026-05-24 14:45 +03: Phase 11m moved search modules onto native typed executors. `TavilyProvider` now owns typed executors for `web_search` and `web_extract`, and `SearxngProvider` owns a typed executor for `searxng_search`. `TavilyToolModule` and `SearxngToolModule` now share provider construction between legacy and typed registration, preserving env/config gating while filling the typed runtime path. Added no-external-network Tavily invalid-argument executor coverage, SearXNG local HTTP typed execution coverage, and typed registry tests for exposed/disabled search module states plus duplicate-registration prevention. Validation passed: `cargo test -p oxide-agent-core providers::tavily --all-features`; `cargo test -p oxide-agent-core --test searxng_provider --features tool-searxng`; `cargo test -p oxide-agent-core typed_runtime_registry_registers_search_modules_once --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_tavily_module --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_searxng_module --all-features`; `cargo check -p oxide-agent-core --no-default-features --features tool-tavily`; `cargo check -p oxide-agent-core --no-default-features --features tool-searxng`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); static search confirms the deleted generic adapter symbols remain absent; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue native typed executor migration for TTS/media/MCP/browser-use/delegation modules, then delete the remaining legacy `ToolRegistry` execution path.
- 2026-05-24 14:53 +03: Phase 11n moved `tool/ytdlp` onto native typed executors. `YtdlpProvider` now owns typed executors for metadata, transcript, search, video download, and audio download tools, shares a serialized execution lock across yt-dlp tools, reuses the existing sandbox-backed execution path with runtime cancellation tokens, and maps malformed JSON arguments to typed invalid-argument errors. `YtdlpToolModule` now shares provider construction between legacy and typed registration, preserving sandbox runtime and progress sender wiring. Added fake sandbox typed execution coverage for `ytdlp_search_videos` plus typed registry coverage for exposed and disabled `tool/ytdlp` states. Validation passed: `cargo test -p oxide-agent-core providers::ytdlp --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_ytdlp_tools --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_ytdlp_module --all-features`; `cargo check -p oxide-agent-core --no-default-features --features tool-ytdlp`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); static search confirms the deleted generic adapter symbols remain absent; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue native typed executor migration for TTS/media/MCP/browser-use/delegation modules, then delete the remaining legacy `ToolRegistry` execution path.
- 2026-05-24 15:04 +03: Phase 11o moved TTS modules onto native typed executors. `KokoroTtsProvider` now owns typed executors for `text_to_speech_en` and `text_to_speech_en_file`; `SileroTtsProvider` owns typed executors for `text_to_speech_ru` and `text_to_speech_ru_file`. `KokoroTtsToolModule` and `SileroTtsToolModule` now share provider construction between legacy and typed registration, preserving empty-URL disable behavior, sandbox runtime wiring, and progress sender wiring. Added typed provider tests plus typed registry coverage for exposed and disabled TTS module states. Validation passed: `cargo test -p oxide-agent-core providers::tts --all-features`; `cargo test -p oxide-agent-core providers::silero_tts --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_tts_tools --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_kokoro_tts_module --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_silero_tts_module --all-features`; `cargo check -p oxide-agent-core --no-default-features --features tool-tts-kokoro`; `cargo check -p oxide-agent-core --no-default-features --features tool-tts-silero`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); static search confirms the deleted generic adapter symbols remain absent; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue native typed executor migration for media/MCP/browser-use/delegation modules, then delete the remaining legacy `ToolRegistry` execution path.
- 2026-05-24 15:13 +03: Phase 11p moved media file-analysis modules onto native typed executors. `MediaFileProvider` now owns typed executors for `transcribe_audio_file`, `describe_image_file`, and `describe_video_file`, with `tool_runtime_executors_for` allowing each capability module to register only its own tool. `MediaAudioToolModule`, `MediaImageToolModule`, and `MediaVideoToolModule` now fill the typed runtime path while preserving the existing filtered legacy provider fallback. Added typed provider tests for full registration, module-local filtering, and invalid-argument normalization plus typed registry coverage for exposed and disabled media module states. Validation passed: `cargo test -p oxide-agent-core providers::media_file --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_media_tools --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_media_audio_module --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_media_image_module --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_media_video_module --all-features`; `cargo check -p oxide-agent-core --no-default-features --features tool-media-audio`; `cargo check -p oxide-agent-core --no-default-features --features tool-media-image`; `cargo check -p oxide-agent-core --no-default-features --features tool-media-video`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); static search confirms the deleted generic adapter symbols remain absent; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue native typed executor migration for MCP/browser-use/delegation/compression modules, then delete the remaining legacy `ToolRegistry` execution path.
- 2026-05-24 15:22 +03: Phase 11q moved Jira and Mattermost MCP integrations onto native typed executors. `JiraMcpProvider` now owns typed executors for `jira_read`, `jira_write`, and `jira_schema`; `MattermostMcpProvider` owns typed executors for the Mattermost tool mapping surface. `JiraMcpToolModule` and `MattermostMcpToolModule` now share provider construction between legacy and typed registration, preserving env/config gating and binary-path logging. Mattermost now parses JSON arguments before MCP client initialization so malformed arguments are rejected without spawning the sidecar binary. Added typed provider tests plus typed registry coverage for exposed, disabled, and duplicate-registration MCP module states. Validation passed: `cargo test -p oxide-agent-core providers::jira_mcp --all-features`; `cargo test -p oxide-agent-core providers::mattermost_mcp --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_jira_mcp_tools_when_configured --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_mattermost_mcp_tools_when_configured --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_jira_mcp_module --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_mattermost_mcp_module --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_registers_mcp_modules_once --all-features`; `cargo check -p oxide-agent-core --no-default-features --features integration-mcp-jira`; `cargo check -p oxide-agent-core --no-default-features --features integration-mcp-mattermost`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); static search confirms the deleted generic adapter symbols remain absent; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue native typed executor migration for browser-use/delegation/compression modules, then delete the remaining legacy `ToolRegistry` execution path.
- 2026-05-24 15:28 +03: Phase 11r moved `tool/browser-use` onto native typed executors. `BrowserUseProvider` now owns typed executors for `browser_use_run_task`, `browser_use_get_session`, `browser_use_close_session`, `browser_use_extract_content`, and `browser_use_screenshot`, reusing the existing request dispatch, cancellation handling, semaphore limit, profile scope, sandbox artifact hydration, and Browser LLM route inheritance logic. `BrowserUseToolModule` now shares provider construction between legacy and typed registration while preserving disabled-by-default env/config gating and profile/sandbox wiring. Added typed provider tests plus typed registry coverage for enabled, disabled-module, and disabled-service Browser Use states. Validation passed: `cargo test -p oxide-agent-core browser_use --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_browser_use_tools_when_enabled --all-features`; `cargo check -p oxide-agent-core --no-default-features --features tool-browser-use`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); static search confirms the deleted generic adapter symbols remain absent; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: resolve remaining `tool/delegation` and runner-handled `tool/compression` typed-runtime gaps, then delete the remaining legacy `ToolRegistry` execution path.
- 2026-05-24 15:40 +03: Phase 11s moved `tool/delegation` onto native typed executors. `DelegationProvider` now owns typed executors for `spawn_sub_agents`, `wait_sub_agents`, and `cancel_sub_agents`, shares the same internal dispatch with the temporary legacy provider path, and carries the module progress sender plus runtime cancellation token into sub-agent spawn handling. The typed delegation executors intentionally avoid a shared execution lock so long-running `wait_sub_agents` calls cannot block `cancel_sub_agents`. `DelegationToolModule` now registers these typed executors directly, and registry coverage proves delegation tools are exposed exactly once and disappear when `modules.tool/delegation.enabled=false`. Validation passed: `cargo test -p oxide-agent-core providers::delegation --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_delegation_tools --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_delegation_module --all-features`; `cargo check -p oxide-agent-core --no-default-features --features tool-delegation`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); static search confirms the deleted generic adapter symbols remain absent; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: resolve runner-handled `tool/compression` typed-runtime gap, then delete the remaining legacy `ToolRegistry` execution path.
- 2026-05-24 15:49 +03: Phase 11t resolved the runner-handled `tool/compression` typed-runtime gap. `CompressionProvider` now owns a native typed executor for `compress`, validates that arguments are an empty JSON object, and returns a paired typed tool output indicating manual context compression has been scheduled. `CompressionToolModule` now registers the executor directly, `current_tool_definitions` exposes `compress` for the v1 typed runtime route, and the runner converts a successful typed `compress` output into `RunState::request_manual_compaction()` so the existing compaction pipeline runs before the next model call. Validation passed: `cargo test -p oxide-agent-core providers::compression --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_sandbox_tools --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_skips_disabled_compression_module --all-features`; `cargo test -p oxide-agent-core typed_runtime_compress_output_requests_manual_compaction --all-features`; `cargo test -p oxide-agent-core current_tool_definitions_use_typed_runtime_specs_for_v1_route --all-features`; `cargo check -p oxide-agent-core --no-default-features --features tool-compression`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); static search confirms the deleted generic adapter symbols remain absent; static search confirms `agent/tool_runtime/modules.rs` has no remaining `Vec::new()` typed executor gaps; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: delete or replace the remaining legacy `ToolRegistry`/`build_tool_registry` execution path and its compatibility tests.
- 2026-05-24 16:01 +03: Phase 11u removed the legacy `ToolRegistry` from the main-agent runner/executor preparation path. `AgentRunnerContext` and `PreparedExecution` no longer carry the legacy registry, main `prepare_execution` now builds tool definitions directly from `RuntimeToolRegistry`, and `current_tool_definitions` always returns typed runtime specs instead of selecting a legacy registry by route. The old `build_tool_registry` construction remains `#[cfg(test)]` for compatibility tests only, while sub-agent delegation still has an internal legacy `ToolRegistry` dependency that must be migrated separately. Validation passed: `cargo test -p oxide-agent-core typed_runtime_path_records_paired_assistant_and_tool_history --all-features`; `cargo test -p oxide-agent-core typed_runtime_compress_output_requests_manual_compaction --all-features`; `cargo test -p oxide-agent-core typed_runtime_path_rejects_unsupported_active_route_without_history_write --all-features`; `cargo test -p oxide-agent-core run_repairs_invalid_tool_history_before_llm_call --all-features`; `cargo test -p oxide-agent-core legacy_registry_skips_disabled_compression_module --all-features`; `cargo test -p oxide-agent-core registry --all-features`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); static search confirms no `registry` field remains on runner contexts and main production execution no longer calls `build_tool_registry`; static search confirms the deleted generic adapter symbols remain absent; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: migrate sub-agent delegation off `agent::registry::ToolRegistry`, then delete `agent/registry.rs`, legacy `ToolProvider` dispatch, and legacy compatibility tests.
- 2026-05-24 16:23 +03: Phase 11v migrated the sub-agent delegation preparation path off the legacy `ToolRegistry`. `PreparedSubAgentExecution` now carries an `Arc<RuntimeToolRegistry>`, sub-agent tools are built from native typed executors and filtered by the requested allowlist at registration time, `AgentRunnerContext` receives the typed registry for sub-agent runs, and the old `RestrictedToolProvider` wrapper was removed. The runner route guard was also narrowed so final-only direct-provider sub-agent runs are not rejected before the LLM call when no opencode-go route exists; unsupported routes still fail before typed tool execution and do not write partial tool history. Validation passed: `cargo test -p oxide-agent-core providers::delegation --all-features`; `cargo test -p oxide-agent-core --test sub_agent_delegation --all-features`; `cargo test -p oxide-agent-core typed_runtime_registry_exposes_delegation_tools --all-features`; `cargo test -p oxide-agent-core typed_runtime_path_rejects_unsupported_active_route_without_history_write --all-features`; `cargo check -p oxide-agent-core --no-default-features --features tool-delegation`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); static search confirms production delegation no longer references `agent::registry::ToolRegistry`, `build_sub_agent_providers`, or `RestrictedToolProvider`; static search confirms the deleted generic adapter symbols remain absent; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: delete the test-only `build_tool_registry` compatibility surface, then remove `agent/registry.rs` and legacy `ToolProvider` dispatch.
- 2026-05-24 16:46 +03: Phase 11w deleted the remaining legacy tool registry compatibility surface. Removed `agent::registry::ToolRegistry`, the test-only `build_tool_registry` builder, legacy registry helper methods, `ToolModule::legacy_provider`, `FilteredToolProvider`, and legacy registry compatibility tests. Executor and manager registry tests now cover the typed runtime registry only while direct provider unit coverage remains in place for provider-local behavior. Static search confirms no source references remain for `agent::registry`, `build_tool_registry`, `legacy_provider`, `FilteredToolProvider`, `gemini_rust`, `gemini-rust`, `provider_runtime_executors`, `ProviderRuntimeExecutor`, or `provider_executor`; remaining `pub mod registry` declarations are the typed runtime registry and hook registry only. Validation passed: `cargo check -p oxide-agent-core --all-features`; `cargo test -p oxide-agent-core registry --all-features`; `cargo test -p oxide-agent-core providers::manager_control_plane::tests::registry --all-features`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue Phase 11 finalization by removing remaining provider-level `ToolProvider` dispatch paths where native typed executors already cover execution, or move to final PRD acceptance guards if direct provider test surfaces are still needed temporarily.
- 2026-05-24 17:02 +03: Phase 11x removed `manager/control-plane` from the legacy `ToolProvider` trait surface. `ManagerControlPlaneProvider` now exposes production execution through native typed executors only; its old `ToolProvider` import/impl was deleted, while provider-local unit tests use `cfg(test)` helpers over the same internal handler. Telegram route and cleanup tests now invoke manager control-plane tools through `tool_runtime_executors()` with typed `ToolInvocation` envelopes instead of importing `ToolProvider`. Validation passed: `cargo test -p oxide-agent-core providers::manager_control_plane --all-features`; `cargo test -p oxide-agent-transport-telegram topic_route::tests::auto_binding_workflow_routes_thread_to_dynamic_binding --all-features`; `cargo test -p oxide-agent-transport-telegram --test manager_topic_delete_cleanup_integration --all-features`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); static search confirms `ToolProvider` no longer appears in manager-control-plane source/tests or the adjusted Telegram tests; `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue removing provider-level `ToolProvider` impls from smaller native-typed providers, then delete the trait/export once no provider or test depends on it.
- 2026-05-24 17:09 +03: Phase 11y removed `tool/compression` and `tool/todos` from the legacy `ToolProvider` trait surface. Deleted both providers' `ToolProvider` imports/impls, removed the obsolete compression legacy-execute test, and converted todos provider tests to drive `write_todos` through native typed executors while preserving progress-event coverage. Static search confirms `compression.rs` and `todos.rs` no longer reference `ToolProvider`, direct provider `.execute(...)`, `can_handle`, or legacy `.tools()` usage. Validation passed: `cargo check -p oxide-agent-core --all-features`; `cargo test -p oxide-agent-core providers::compression --all-features`; `cargo test -p oxide-agent-core providers::todos --all-features`; `cargo check --workspace`; all six workspace profile checks (`profile-full`, `profile-embedded-opencode-local`, `profile-lite`, `profile-search-only`, `profile-no-sandbox`, `profile-media-enabled`); `cargo fmt --all --check`; and `cargo clippy --workspace --all-targets --all-features`. Next: continue removing `ToolProvider` impls from native-typed low-risk providers such as `stack_logs`, `webfetch_md`, `agents_md`, and `wiki_memory`, then remove the trait/export once all remaining tests are typed-runtime based.

## Risks and Blockers

- The PRD intentionally rejects compatibility and migrations; deletion phases must be sequenced behind replacement modules to keep the workspace buildable.
- The PRD contains early examples using durable local storage, but section 22.7 later decides against it. This goal follows the later explicit decision.
- Moving dependencies to optional features can temporarily expose many unconditional imports; use small cfg boundaries and profile checks instead of large speculative rewrites.
- Docker/Compose profile generation depends on module requirement metadata that does not exist yet; early Docker work should stay profile-specific until manifests can drive generation.

## Final Verification

- Pending.
